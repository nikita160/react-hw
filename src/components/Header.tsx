import { FC } from 'react';
import styles from './Header.module.css';

const Header: FC = () => {
  return <header className={styles.header}>GitHub reviewer finder</header>;
};

export default Header;
